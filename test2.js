function accum(str) {
   var test = str.split('')
// var test1 = test.map(x => x.indexOf(x))
//    console.log(typeof test)
//    console.log(test)
    test.forEach(element => {
        return element
    });

    const logArray = (element, index) => {
        console.log(index)
    }
}

accum("abcd")